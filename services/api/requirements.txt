# TeeDesk API — Python dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database (PostgreSQL + pgvector)
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10
pgvector==0.3.6

# Redis (caching + sessions + Celery broker)
redis==5.2.1
celery==5.4.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-multipart==0.0.20

# Rate limiting
slowapi==0.1.9

# Validation
pydantic[email]==2.10.4
pydantic-settings==2.7.0
eval-type-backport==0.2.0

# HTTP client (for Ollama, webhooks)
httpx==0.28.1

# Email (SMTP — verification, password reset)
aiosmtplib==5.1.3

# NLP / AI pipeline
# torch 2.5.x has no Intel Mac wheel — latest available for x86_64 macOS is 2.2.2
torch==2.2.2
transformers==4.44.2
sentence-transformers==2.7.0
spacy==3.8.3
scipy==1.14.1
numpy==1.26.4

# Document parsing (KB article ingestion — PDF/DOCX upload)
pypdf==5.1.0
python-docx==1.1.2

# Environment
python-dotenv==1.0.1

# Observability
structlog==24.4.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
